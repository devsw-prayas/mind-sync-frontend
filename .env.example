# Copy to .env.local for local dev, or set in Vercel project settings.
#
# Leave EMPTY to run entirely on the in-memory mock (src/api/mock.js).
# Set to your backend origin to switch every api call to real HTTP.
VITE_API_URL=
# e.g. VITE_API_URL=https://mindsync-api.example.com
